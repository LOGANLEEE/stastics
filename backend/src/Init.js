const crawl = require('./crawl');
const preProcessor = require('./preProcessor');

// setInterval(() => {
//     info(`=================`);
//     Crawl.init(),  10000// 10s
// });

function start() {
	// info(`=================`);
	// crawl.init(); // 10s
	// preProcessor.exec();
}

module.exports = {
	start,
};
